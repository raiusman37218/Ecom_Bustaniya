export const metadata = {
  title: "Checkout - Bustaniya",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
