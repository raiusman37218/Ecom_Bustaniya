export const metadata = {
  title: "Checkout - Lisette",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
