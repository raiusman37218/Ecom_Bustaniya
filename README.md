# Bustaniya Store

Reusable Next.js ecommerce storefront, prepared for a future Supabase backend.

## Run

1. `npm install`
2. `npm run dev`
3. Open `http://localhost:3000`

Products are currently editable in `data/store.js`.
